import { Vector3 } from "three";

export const sceneVectors = {
    initialCameraPosition: new Vector3(200, 200, 200),
    mainStage: new Vector3(),
    secondStage: new Vector3(),
    thirdStage: new Vector3(),
    ground: new Vector3()
}